# Server Configuration
server.port=${PORT:8080}
server.error.include-message=always
server.error.include-binding-errors=always

# MongoDB Configuration - USE ENVIRONMENT VARIABLE
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://Rithwik_1810:Rithwik%401810@cluster0.eh2zxzp.mongodb.net/lecsumm?retryWrites=true&w=majority&appName=Cluster0}

# File Upload Configuration
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB
file.upload-dir=./uploads

# JWT Configuration
jwt.secret=${JWT_SECRET:mysecretkeythatneedstobeatleast256bitslong}
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://lecsumm.indevs.in}

# Allow circular references (temporary fix)
spring.main.allow-circular-references=true

# Logging
logging.level.com.cvr.cse=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb.core=DEBUG
# Python AI Service - set via environment variable
ai.service.url=${AI_SERVICE_URL:http://localhost:5000}

# Google Auth Configuration
google.client.id=${GOOGLE_CLIENT_ID:646426743846-qu5hrl9boiu660qp5kq3hsaj9cne07no.apps.googleusercontent.com}

